{
  "name": "smart-scroller",
  "version": "0.2.0",
  "private": true,
  "description": "A local WebExtension that combines feed steering with explainable low-signal filtering.",
  "scripts": {
    "test": "node --test"
  }
}
