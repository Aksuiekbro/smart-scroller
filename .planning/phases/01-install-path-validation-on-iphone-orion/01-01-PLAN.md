---
phase: 01-install-path-validation-on-iphone-orion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/install-ios.md
  - README.md
  - .gitignore
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
must_haves:
  truths:
    - "Author can re-install v0.1.1 on iPhone Orion from scratch in under 10 minutes by following docs/install-ios.md"
    - "docs/install-ios.md documents the locked Install-from-File path (D-01) as primary, with CWS-unlisted and Xcode-wrapped paths labeled as backup recipes"
    - "docs/install-ios.md tells the user to enable Request Desktop Website in Orion (D-10) and explains that without it only Instagram works on iPhone today"
    - "README.md no longer claims CWS-unlisted is the recommended Orion path; it points readers at docs/install-ios.md for iPhone Orion install"
    - "Built .zip artifacts are not committed to the repo going forward"
  artifacts:
    - path: "docs/install-ios.md"
      provides: "iPhone Orion install + re-install checklist (PLAT-02)"
      contains: "Install from File"
      min_lines: 50
    - path: "README.md"
      provides: "Updated Install in Orion section pointing at docs/install-ios.md"
      contains: "docs/install-ios.md"
    - path: ".gitignore"
      provides: "*.zip exclusion so future builds don't get committed"
      contains: "*.zip"
  key_links:
    - from: "README.md"
      to: "docs/install-ios.md"
      via: "relative markdown link"
      pattern: "docs/install-ios\\.md"
    - from: "docs/install-ios.md"
      to: ".planning/phases/01-install-path-validation-on-iphone-orion/01-CONTEXT.md"
      via: "decisions D-01 through D-13 are the source of truth"
      pattern: "Install from File"
---

## Phase Goal

**As a** SmartScroller author re-installing the extension on a fresh iPhone, **I want to** follow a single in-repo checklist that takes me from cloned source to a visibly-blurring extension in Orion iOS, **so that** the install path discovered on 2026-05-16 is not lost knowledge the next time the phone is reset or Orion changes.

<objective>
Close Phase 1 by writing the doc that lets a future SmartScroller install on iPhone Orion be a checklist instead of re-discovery, updating the README to stop pointing at the stale CWS-unlisted advice, and stopping built .zip artifacts from being committed.

Purpose: PLAT-01/PLAT-03/PLAT-04 were organically delivered during research and debug work (verified on-device 2026-05-16, accepted under pragmatic-close D-08). PLAT-02 — the re-installable checklist — is the only criterion still open. This plan writes it, fixes one stale README section that contradicts it, and tightens the repo so v0.1.x and beyond don't accumulate zip artifacts. No code touched. No new agents. No research.

Output: `docs/install-ios.md` (new, ~50–80 lines, 7 sections per D-06), updated `README.md` "Install in Orion" section, `.gitignore` with `*.zip` excluded and the two existing zips removed from the index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-install-path-validation-on-iphone-orion/01-CONTEXT.md
@README.md
@manifest.json

<interfaces>
<!-- No code interfaces — this is a docs + repo-hygiene plan. -->
<!-- The "interface" the executor must honor is CONTEXT.md D-01 through D-13. -->
<!-- Source of truth for prose decisions: 01-CONTEXT.md <decisions> block. -->

CONTEXT.md decisions the executor MUST honor verbatim:
- D-01: Locked install path = "Install from File" via Orion iOS Extensions menu, fed a self-built zip.
- D-02: CWS-unlisted ($5) and Xcode-wrapped ($99/yr) are RULED OUT as primary; kept as backup recipes only.
- D-03: Zip-build command = `zip -r smartscroller-vX.Y.Z.zip manifest.json background/ content/ options/ popup/ -x "*.DS_Store"`
- D-06: Doc scope is 7 sections (zip-build cmd / transfer paths / Orion install steps / Request Desktop prereq / smoke-test checklist / troubleshooting / backup recipes).
- D-07: Target length ~50–80 lines; lives at `docs/install-ios.md`.
- D-10: Request Desktop Website is a documented user prerequisite on iPhone Orion.
- D-12: README "Install in Orion" section is currently stale (recommends CWS-unlisted as primary); fix it to point at the new doc.

README.md voice contract (from CONTEXT.md code_context):
- Terse, sectioned, lots of fenced code blocks, explicit numbered steps.
- No npm/build-step language anywhere (project has no toolchain).
- `globalThis.browser ?? globalThis.chrome` polyfill is not affected by install path; do not mention it in install docs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/install-ios.md per CONTEXT.md D-06/D-07</name>
  <files>docs/install-ios.md</files>
  <read_first>
    - .planning/phases/01-install-path-validation-on-iphone-orion/01-CONTEXT.md (especially <decisions> D-01 through D-13 and <specifics>)
    - README.md (voice/style reference — terse, numbered, fenced code blocks)
    - manifest.json (confirm current version 0.1.1 to use in zip filename example)
    - .planning/ROADMAP.md Phase 1 block (Goal + Success Criteria 1–5; SC#5 already locks the Install-from-File path)
  </read_first>
  <action>
    Create docs/install-ios.md (file does not exist yet). Target ~50–80 lines per D-07. Match README.md voice: terse, sectioned, fenced code blocks, numbered steps. The doc MUST contain the 7 sections enumerated in D-06, in this order (use these as the H2 outline; exact wording is Claude's discretion per CONTEXT.md):

    1. Zip-build command — verbatim from D-03 including the `-x "*.DS_Store"` exclusion. Use `smartscroller-v0.1.1.zip` as the filename example since manifest is at v0.1.1. Run from repo root.
    2. Transfer paths to iPhone — AirDrop preferred, iCloud Drive secondary, email-to-self as last resort. One sentence each on why.
    3. Orion iOS install steps — use the EXACT UI labels per CONTEXT.md <specifics>: "Menu → Extensions → ↓ → Install from File → select zip". Numbered steps. No fluff.
    4. Required user step: enable Request Desktop Website (per D-10). Explain the "AA" icon / PageMenu location. Single sentence on WHY: without it only Instagram works on iPhone today, and Phase 2 lifts this requirement.
    5. Smoke-test checklist (~5 items per D-06). Start with the 30-second verification per CONTEXT.md <specifics>: "open youtube.com/shorts/, scroll one Short with Request Desktop on, see a frosted overlay if the Short isn't AI/programming-related." Include: tap Show anyway → reveal works, open Instagram Reels → blur fires, popup counter increments.
    6. Troubleshooting — three subsections: (a) "Install from File" rejects the zip → suggest unpacked dir / CRX-pack as fallback; (b) blur doesn't appear → check Request Desktop, check enable toggle in popup, check storage.sync; (c) extension disappears after restart → confirm Orion's extensions list still shows it, otherwise reinstall.
    7. Backup install recipes — short notes only, per CONTEXT.md <specifics>: CWS-unlisted ($5, link to Google docs, no full how-to) and Xcode-wrapped Safari Web Extension ($99/yr, mention as last resort). Both labeled explicitly as "fallback only — use Install from File first."

    Open the doc with a one-line summary stating this is the verified Orion iOS install path as of 2026-05-16 (per D-01). Do NOT include npm/build-step language anywhere. Do NOT mention the `globalThis.browser ?? globalThis.chrome` polyfill. Do NOT pad with explanations of what an extension is — the audience is the author.
  </action>
  <verify>
    <automated>test -f docs/install-ios.md &amp;&amp; wc -l docs/install-ios.md | awk '{if ($1 &lt; 40 || $1 &gt; 120) {print "FAIL: line count " $1 " outside 40-120 range"; exit 1} else {print "OK lines:" $1}}' &amp;&amp; grep -q "Install from File" docs/install-ios.md &amp;&amp; grep -q "Request Desktop" docs/install-ios.md &amp;&amp; grep -q 'zip -r' docs/install-ios.md &amp;&amp; grep -q -- "-x" docs/install-ios.md &amp;&amp; grep -q "Show anyway" docs/install-ios.md &amp;&amp; grep -qiE "(troubleshoot|troubleshooting)" docs/install-ios.md &amp;&amp; (grep -qiE "(backup|fallback)" docs/install-ios.md) &amp;&amp; (grep -qiE "(chrome web store|cws)" docs/install-ios.md) &amp;&amp; (grep -qiE "(xcode|safari web extension)" docs/install-ios.md) &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - docs/install-ios.md exists at repo root under docs/
    - File length is 40–120 lines (target 50–80 per D-07, tolerance for prose variation)
    - Contains the literal string "Install from File" (D-01 UI label)
    - Contains the literal string "Request Desktop" (D-10 user prerequisite)
    - Contains a `zip -r ...` fenced code block with `-x` exclusion (D-03)
    - Contains "Show anyway" in the smoke-test section (D-06 §5)
    - Contains a Troubleshooting section
    - Contains a Backup / Fallback section that mentions both Chrome Web Store (CWS) and Xcode-wrapped Safari Web Extension, labeled as fallback only (D-02)
    - Voice matches README.md: numbered steps, fenced code blocks, no toolchain language
    - File renders cleanly as Markdown (no broken headings, no orphan list markers)
  </acceptance_criteria>
  <done>
    docs/install-ios.md committed. Reading it top-to-bottom produces the same on-device install outcome the author achieved 2026-05-16 with no inference required. PLAT-02 closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace stale "Install in Orion" section in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content; the "Install in Orion" section is at roughly lines 19–30 and recommends CWS-unlisted as primary — this is stale per D-12)
    - docs/install-ios.md (the file Task 1 just created — README must link to it)
    - .planning/phases/01-install-path-validation-on-iphone-orion/01-CONTEXT.md D-12
  </read_first>
  <action>
    Edit the existing "## Install in Orion" section of README.md (currently around lines 19–30, which currently advises Chrome Web Store Unlisted as the workable path). Replace it with a short section that:

    1. States the verified path is "Install from File" via Orion iOS (per D-01, verified 2026-05-16).
    2. Points readers at `docs/install-ios.md` for the full checklist (single relative markdown link).
    3. Keeps the "Install in Chrome / Edge / Brave (works today)" subsection that follows — it's still accurate and PLAT-03 accepts the dev sandbox as-is per D-08. Do not touch it.
    4. Keeps the "Install in Firefox" subsection — also still accurate.

    Target length for the rewritten "Install in Orion" subsection: 5–12 lines (one paragraph + a link line + a one-line note about the Request Desktop prerequisite). Do NOT duplicate the install-ios.md content here. Do NOT remove the Chrome/Brave or Firefox subsections. Do NOT change any other section of README.md (the "What it does", "Configure topics", "How classification works", "Upgrade path", "Files", "Known gotchas", "Roadmap", "License" sections stay verbatim).
  </action>
  <verify>
    <automated>grep -q "docs/install-ios.md" README.md &amp;&amp; grep -q "Install from File" README.md &amp;&amp; ! grep -qE "publish privately to the Chrome Web Store as \*\*Unlisted\*\*" README.md &amp;&amp; grep -q "Install in Chrome / Edge / Brave" README.md &amp;&amp; grep -q "Install in Firefox" README.md &amp;&amp; grep -q "## Configure topics" README.md &amp;&amp; grep -q "## How classification works" README.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a relative link to `docs/install-ios.md`
    - README.md contains the string "Install from File" (the locked path)
    - README.md no longer contains the stale "publish privately to the Chrome Web Store as **Unlisted**" recommendation as the primary Orion path
    - README.md still contains the "Install in Chrome / Edge / Brave (works today)" subsection untouched
    - README.md still contains the "Install in Firefox" subsection untouched
    - README.md still contains "## Configure topics" and "## How classification works" sections untouched (smoke check for accidental wipe)
    - No other section of README.md modified
  </acceptance_criteria>
  <done>
    README.md "Install in Orion" section is current with the locked decision (D-12) and refers readers to docs/install-ios.md for the full procedure. The stale CWS-unlisted-as-primary advice is gone. Chrome/Brave and Firefox dev-sandbox instructions preserved (PLAT-03 documentation).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add *.zip to .gitignore and untrack the committed zip artifacts</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (currently does not exist at repo root — confirm with `ls .gitignore`)
    - Repo root listing: `smartscroller-v0.1.1.zip` and `smartscroller.zip` are both currently tracked by git (per CONTEXT.md <code_context> note that both v0.1.0 and v0.1.1 zips were committed)
    - .planning/phases/01-install-path-validation-on-iphone-orion/01-CONTEXT.md <decisions> "Claude's Discretion" — `.gitignore` addition for `*.zip` is approved (decision is to do it, per the orchestrator's critical_context_for_planner item 2)
  </read_first>
  <action>
    Create .gitignore at repo root (it does not exist). Add a single line: `*.zip`. Then remove the two existing zip artifacts from the git index WITHOUT deleting them from the working tree, so the local build outputs remain on disk but are no longer tracked:

    - `git rm --cached smartscroller-v0.1.1.zip`
    - `git rm --cached smartscroller.zip`

    Do not commit yet — the execute-phase orchestrator handles commit. Do not delete the zip files from disk (they remain useful local artifacts for the author). Do not add any other ignore patterns; this is a single-purpose hygiene change for Phase 1.
  </action>
  <verify>
    <automated>test -f .gitignore &amp;&amp; grep -qx '\*\.zip' .gitignore &amp;&amp; git ls-files smartscroller-v0.1.1.zip | grep -q . &amp;&amp; { echo "FAIL: smartscroller-v0.1.1.zip still tracked"; exit 1; } ; git ls-files smartscroller.zip | grep -q . &amp;&amp; { echo "FAIL: smartscroller.zip still tracked"; exit 1; } ; test -f smartscroller-v0.1.1.zip &amp;&amp; test -f smartscroller.zip &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore exists at repo root
    - .gitignore contains the exact line `*.zip` (no other patterns required)
    - `git ls-files smartscroller-v0.1.1.zip` returns empty (untracked)
    - `git ls-files smartscroller.zip` returns empty (untracked)
    - Both zip files still exist on disk (not deleted from working tree)
    - `git status` shows .gitignore as a new tracked file and the two zip removals as staged deletions
  </acceptance_criteria>
  <done>
    Future `git status` after `zip -r ...` will not surface the build artifact. The two pre-existing zips are out of the index but kept on disk for the author's reference. Repo hygiene closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local FS → git index | Build artifacts (zip files) crossing into the committed repo |
| Repo docs → future installer (human author) | Stale documentation misleading the author into wasting time on the wrong install path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | committed zip artifacts | accept | Zips contain only the same files already in the repo (no secrets, no user data). Untracking them via Task 3 is hygiene, not a real disclosure risk. |
| T-01-02 | Tampering | docs/install-ios.md misleading author | mitigate | Task 1 sources every step from CONTEXT.md D-01 through D-13 verbatim (UI labels, zip command, Request Desktop prereq); no inference allowed. Verify gates check for the literal strings. |
| T-01-03 | Repudiation | README stale advice contradicting docs/install-ios.md | mitigate | Task 2 removes the stale CWS-unlisted-as-primary text and links to the new doc as the single source of truth. Grep gate enforces both presence of new link and absence of old phrasing. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages. No package manager touched. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
After all three tasks complete:

1. `test -f docs/install-ios.md` returns success
2. `wc -l docs/install-ios.md` shows 40–120 lines
3. `grep -c "Install from File" docs/install-ios.md README.md` shows hits in both files
4. `grep "publish privately to the Chrome Web Store" README.md` returns empty (stale text gone)
5. `test -f .gitignore && grep -x '*.zip' .gitignore` succeeds
6. `git ls-files | grep -E '\.zip$'` returns empty
7. Manual sanity read: skim docs/install-ios.md end-to-end and confirm the 7 D-06 sections are present in order
</verification>

<success_criteria>
- [ ] docs/install-ios.md exists, 40–120 lines, 7 D-06 sections present, references Install from File + Request Desktop + zip-build command
- [ ] README.md "Install in Orion" section points at docs/install-ios.md and no longer recommends CWS-unlisted as the primary Orion path
- [ ] README.md "Install in Chrome / Edge / Brave" and "Install in Firefox" subsections preserved unchanged (PLAT-03 dev sandbox documentation intact)
- [ ] .gitignore exists with `*.zip` entry
- [ ] smartscroller-v0.1.1.zip and smartscroller.zip untracked but still on disk
- [ ] Phase 1 SC#1 (visible blur on iPhone) — already DONE pre-plan per CONTEXT.md (no task required)
- [ ] Phase 1 SC#2 (re-installable doc) — DONE by Task 1
- [ ] Phase 1 SC#3 (dev sandbox preserved) — DONE pre-plan per D-08, documentation preserved by Task 2
- [ ] Phase 1 SC#4 (survives lifecycle) — accepted pre-plan per D-08/D-09, formal coverage deferred to Phase 4 smoke playbook
- [ ] Phase 1 SC#5 (locked install path documented in ROADMAP) — DONE pre-plan in commit 71aff45 / 2e4cb42
- [ ] No new code, no agents spawned, no research; this phase closes as docs-and-hygiene
</success_criteria>

<output>
Create `.planning/phases/01-install-path-validation-on-iphone-orion/01-01-SUMMARY.md` when done. Note in the SUMMARY which PLAT requirements this plan closed (PLAT-02 directly; PLAT-01/03/04 documentation-only support).
</output>
